# Application Configuration
spring.application.name=employee-management-system

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/employee_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Saurya@22
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server Configuration
server.port=8081

# JWT Configuration
app.jwt.secret=EmployeeManagementSystemSecretKeyForJWTTokenGenerationAndValidation2024
app.jwt.expiration-ms=60000

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.employee=DEBUG
